# Optional future server integration. The current Raahi demo uses built-in guidance
# and makes no OpenAI API request. Never prefix this secret with VITE_.
OPENAI_API_KEY=

# Optional server-side model override.
OPENAI_CHAT_MODEL=gpt-5.4-mini
